name: retro_budget
description: Simple budget app (one-file version)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  path_provider: ^2.1.4

flutter:
  uses-material-design: true
